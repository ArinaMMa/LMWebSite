{% block body %}

    {# TODO : modifier le titre selon qui ouvre la page (nom proprio / admin) #}
    {# TODO : menu déroulant pour attribuer un propriétaire pour l'administratrice ET intégration directe de l'id client quand il est co #}
    {# TODO : menu déroulant pour la race de cheval #}



    {{ form_start(form, {attr: {class: 'card w-50 mx-auto' }}) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Créer la fiche</button>
    {{ form_end(form) }}
    
{% endblock %}